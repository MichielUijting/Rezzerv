# Clean sync (minimal OCR/kassabon basis)

Bron: lokale Rezzerv zip (gefilterd)

IN SCOPE:
- backend/app (receipt parsing + OCR + status)
- baseline/receipts
- receipt testing & baseline V4

EXCLUDED:
- backend/data/**
- raw images
- OCR debug output
- sqlite database

Doel: stabiele basis voor kassabon parsing en OCR verbeteringen.
