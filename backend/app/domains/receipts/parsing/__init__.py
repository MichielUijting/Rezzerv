"""Receipt parsing domain modules.

Release 0 keeps the legacy parser behaviour intact. These modules expose focused
parsing responsibilities from ``app.services.receipt_service`` so later parser
improvements can be made per responsibility instead of in one large service.
"""
