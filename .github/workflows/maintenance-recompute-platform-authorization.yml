name: Maintenance recompute platform authorization validation

on:
  pull_request:
    paths:
      - 'backend/app/session_entrypoint.py'
      - 'backend/app/services/maintenance_recompute_route_authorization.py'
      - 'backend/tests/test_maintenance_recompute_platform_authorization.py'
      - '.github/workflows/maintenance-recompute-platform-authorization.yml'

jobs:
  maintenance-recompute-platform-authorization:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install focused test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest fastapi pydantic sqlalchemy httpx

      - name: Compile maintenance authorization boundary and tests
        run: |
          python -m py_compile \
            app/session_entrypoint.py \
            app/services/maintenance_recompute_route_authorization.py \
            tests/server_session_route_guard_audit.py \
            tests/test_maintenance_recompute_platform_authorization.py \
            tests/test_kassa_diagnostic_platform_authorization.py \
            tests/test_test_orchestration_platform_route_authorization.py

      - name: Run maintenance permission cutover tests
        env:
          PYTHONPATH: .
          DATABASE_URL: 'sqlite:///:memory:'
        run: |
          pytest -q \
            tests/test_maintenance_recompute_platform_authorization.py \
            tests/test_kassa_diagnostic_platform_authorization.py \
            tests/test_test_orchestration_platform_route_authorization.py

      - name: Run server session route guard audit
        env:
          PYTHONPATH: .
          DATABASE_URL: 'sqlite:///:memory:'
        run: python tests/server_session_route_guard_audit.py
