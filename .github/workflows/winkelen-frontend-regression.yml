name: Winkelen frontend regression

on:
  pull_request:
    paths:
      - 'frontend/src/features/shopping/**'
      - 'frontend/tests/e2e/winkelen.frontend-regression.spec.js'
      - 'frontend/tests/e2e/auth.setup.js'
      - 'frontend/tests/e2e/helpers/**'
      - 'frontend/playwright.config.js'
      - 'frontend/package.json'
      - 'frontend/package-lock.json'
      - 'backend/app/api/shopping_list_routes.py'
      - 'backend/app/services/shopping_list_service.py'
      - 'backend/app/services/authorization_foundation_service.py'
      - 'backend/app/services/system_superuser_session_provisioning.py'
      - 'backend/tests/authorization_role_matrix_selftest.py'
      - 'backend/tests/shopping_list_release1_selftest.py'
      - 'docker-compose.yml'
      - '.github/workflows/winkelen-frontend-regression.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: winkelen-frontend-regression-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  winkelen-regression:
    name: Winkelen Playwright regression
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      PLAYWRIGHT_BASE_URL: http://host.docker.internal:5174
      PLAYWRIGHT_API_URL: http://host.docker.internal:8011
      PLAYWRIGHT_TEST_ADMIN_EMAIL: test-admin@rezzerv.local
      PLAYWRIGHT_HOUSEHOLD_ID: '0'
      REZZERV_TEST_SUPERUSER_EMAIL: supergebruiker@rezzerv.local
      REZZERV_TEST_SUPERUSER_PASSWORD: ${{ secrets.REZZERV_TEST_SUPERUSER_PASSWORD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate temporary test-admin password
        id: credentials
        shell: bash
        run: |
          password="Rt$(python - <<'PY'
          import secrets
          print(secrets.token_hex(24))
          PY
          )"
          echo "PLAYWRIGHT_TEST_ADMIN_PASSWORD=$password" >> "$GITHUB_ENV"
          echo "REZZERV_TEST_ADMIN_PASSWORD=$password" >> "$GITHUB_ENV"
          echo "REZZERV_TEST_ADMIN_EMAIL=test-admin@rezzerv.local" >> "$GITHUB_ENV"
          echo "::add-mask::$password"

      - name: Build and start Rezzerv
        run: docker compose up -d --build

      - name: Wait for backend health
        shell: bash
        run: |
          for attempt in $(seq 1 36); do
            if curl --fail --silent http://localhost:8011/api/health >/dev/null; then
              echo "Backend health is groen."
              exit 0
            fi
            echo "Healthcheck poging $attempt/36..."
            sleep 10
          done
          docker compose ps
          docker compose logs backend
          exit 1

      - name: Validate authorization matrix
        run: |
          docker compose exec -T \
            -e REZZERV_TEST_SUPERUSER_EMAIL="$REZZERV_TEST_SUPERUSER_EMAIL" \
            -e REZZERV_TEST_SUPERUSER_PASSWORD="$REZZERV_TEST_SUPERUSER_PASSWORD" \
            -e REZZERV_TEST_ADMIN_EMAIL="$REZZERV_TEST_ADMIN_EMAIL" \
            -e REZZERV_TEST_ADMIN_PASSWORD="$REZZERV_TEST_ADMIN_PASSWORD" \
            backend python /app/tests/authorization_role_matrix_selftest.py

      - name: Run Winkelen backend selftest
        run: |
          docker compose exec -T \
            -e PYTHONPATH=/app \
            backend python /app/tests/shopping_list_release1_selftest.py

      - name: Prepare household-zero fixtures
        run: docker compose exec -T backend python /app/tests/household_zero_regression_fixture.py prepare

      - name: Run targeted Winkelen Playwright test
        shell: bash
        run: |
          docker run --rm \
            --add-host=host.docker.internal:host-gateway \
            -e PLAYWRIGHT_BASE_URL="$PLAYWRIGHT_BASE_URL" \
            -e PLAYWRIGHT_API_URL="$PLAYWRIGHT_API_URL" \
            -e PLAYWRIGHT_TEST_ADMIN_EMAIL="$PLAYWRIGHT_TEST_ADMIN_EMAIL" \
            -e PLAYWRIGHT_TEST_ADMIN_PASSWORD="$PLAYWRIGHT_TEST_ADMIN_PASSWORD" \
            -e PLAYWRIGHT_HOUSEHOLD_ID="$PLAYWRIGHT_HOUSEHOLD_ID" \
            -v "$GITHUB_WORKSPACE/frontend:/work" \
            -w /work \
            mcr.microsoft.com/playwright:v1.58.2-noble \
            bash -lc "npm ci && ./node_modules/.bin/playwright test tests/e2e/winkelen.frontend-regression.spec.js"

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: winkelen-playwright-report
          path: frontend/playwright-report
          if-no-files-found: ignore
          retention-days: 7

      - name: Cleanup household-zero fixtures
        if: always()
        run: docker compose exec -T backend python /app/tests/household_zero_regression_fixture.py cleanup || true

      - name: Show container logs on failure
        if: failure()
        run: |
          docker compose ps
          docker compose logs --no-color backend frontend

      - name: Stop containers
        if: always()
        run: docker compose down --volumes
